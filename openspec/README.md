# OpenSpec

The v7.1.0 specs have been archived to `archive/v7/`. They describe a superseded
architecture (single-agent, internal graph/LSP/AST tools) and do not reflect the
current v10 coordinator + team dispatch + GitNexus architecture.

New specs for the current architecture are pending.

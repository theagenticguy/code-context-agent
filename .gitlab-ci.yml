stages:
  - lint
  - test
  - bump
  - build-docs
  - pages
  - build
  - release

variables:
  UV_CACHE_DIR: .uv-cache

# =============================================================================
# Lint & Type Check
# =============================================================================

lint:
  stage: lint
  image: ghcr.io/astral-sh/uv:0.9-python3.13-bookworm-slim
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends gcc libc6-dev graphviz libgraphviz-dev pkg-config
  script:
    - uv sync --group dev
    - uv run ruff check src/
    - uv run ruff format --check src/
    - uv run ty check src/
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"
  cache:
    key:
      files:
        - uv.lock
    paths:
      - $UV_CACHE_DIR

# =============================================================================
# Tests
# =============================================================================

test:
  stage: test
  image: ghcr.io/astral-sh/uv:0.9-python3.13-bookworm-slim
  variables:
    GIT_DEPTH: 0
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends git gcc libc6-dev graphviz libgraphviz-dev pkg-config
  script:
    - uv sync --group dev
    - uv run pytest --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml
    expire_in: 1 hour
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"
  cache:
    key:
      files:
        - uv.lock
    paths:
      - $UV_CACHE_DIR

# =============================================================================
# Version Bump (manual trigger → pushes bump commit + tag → triggers release)
# =============================================================================
# Prerequisites:
#   1. Create a project access token with write_repository scope
#   2. Add it as CI/CD variable RELEASE_TOKEN (masked, protected)
#   3. The token push creates the version tag, which triggers build + release

bump:
  stage: bump
  image: debian:bookworm-slim
  variables:
    GIT_DEPTH: 0
    GIT_STRATEGY: clone
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends git curl ca-certificates bash
    - curl https://mise.run | sh
    - export PATH="$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH"
    - mise settings set disable_tools "gitleaks,semgrep"
    - mise install
    - uv sync --group dev
  script:
    - git config user.name "gitlab-ci[bot]"
    - git config user.email "ci@${CI_SERVER_HOST}"
    - git remote set-url origin "https://release-bot:${RELEASE_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
    - git checkout ${CI_COMMIT_BRANCH}
    - mise run release
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
      allow_failure: true
  cache:
    key:
      files:
        - uv.lock
    paths:
      - $UV_CACHE_DIR

# =============================================================================
# Documentation Build
# =============================================================================

build-docs:
  stage: build-docs
  image: ghcr.io/astral-sh/uv:0.9-python3.13-bookworm-slim
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends git gcc libc6-dev graphviz libgraphviz-dev pkg-config
  script:
    - uv sync --group docs
    - uv run mkdocs build
  artifacts:
    paths:
      - site/
    expire_in: 1 hour
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - docs/**/*
        - mkdocs.yml
        - src/**/*.py
  cache:
    key:
      files:
        - uv.lock
    paths:
      - $UV_CACHE_DIR

# =============================================================================
# GitLab Pages Deployment
# =============================================================================

pages:
  stage: pages
  image: busybox:latest
  dependencies:
    - build-docs
  script:
    - mv site public
  artifacts:
    paths:
      - public/
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"
  environment:
    name: production
    url: https://lalsaado.gitlab.aws.dev/code-context-agent

# =============================================================================
# Build Package
# =============================================================================

build:
  stage: build
  image: ghcr.io/astral-sh/uv:0.9-python3.13-bookworm-slim
  script:
    - uv build
  artifacts:
    paths:
      - dist/
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"
  cache:
    key:
      files:
        - uv.lock
    paths:
      - $UV_CACHE_DIR

# =============================================================================
# Release (on tags only)
# =============================================================================

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  dependencies:
    - build
  script:
    - VERSION=$(echo "$CI_COMMIT_TAG" | sed 's/^v//')
    - sed -n "/^## v${VERSION}/,/^## v/{/^## v/d;p}" CHANGELOG.md > /tmp/notes.md
    - |
      if [ ! -s /tmp/notes.md ]; then
        echo "Release ${CI_COMMIT_TAG}" > /tmp/notes.md
      fi
    - release-cli create
        --name "Release ${CI_COMMIT_TAG}"
        --tag-name "${CI_COMMIT_TAG}"
        --description "$(cat /tmp/notes.md)"
        --assets-link "{\"name\":\"Python wheel\",\"url\":\"${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build\",\"link_type\":\"package\"}"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
